<nav class="top-nav sans">
  <a href="<%= path_to(".") %>">❧</a>
</nav>

<article>
  <h1><%= @post.title %></h1>
  <p class="subtitle"><%= @post.date %></p>

  <section>
    <%= @post.body %>
  </section>
</article>
